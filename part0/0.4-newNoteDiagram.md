```mermaid
sequenceDiagram
    participant browser
    participant server
    Browser->>Server: Browser sends user input via POST request to the server
    Server-->>: Responds with HTTP status code 302.
    Note right of browser: The status code 302 is a URL redirect which the server uses to ask the browser to do a new HTTP GET request to the address defined in         the header location 
```

